Wine Launcher Creator can be installed anywhere in the PATH, eg. /usr/local/bin/
(just copy it there as root), or anywhere in the home directory.

To install it in default path, run

sudo make install

Options to integrate Wine Launcher Creator will only work if it is installed in default path.
